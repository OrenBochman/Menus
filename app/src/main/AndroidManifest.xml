<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oren.menus">

    <application
        android:fullBackupContent="true"
        android:allowBackup="true"
        android:icon="@drawable/ic_film"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_film"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".EditActivity">
            <!--<intent-filter android:label="@string/filter_title_editMovie">-->
                <!--<action android:name="android.intent.action.VIEW" />-->
                <!--<category android:name="android.intent.category.DEFAULT" />-->
                <!--<category android:name="android.intent.category.BROWSABLE" />-->
                <!--&lt;!&ndash; Accepts URIs that begin with "http://www.tmdb.com/movies” &ndash;&gt;-->
                <!--<data android:scheme="http"-->
                    <!--android:host="www.tmdb.com"-->
                    <!--android:pathPrefix="/movies" />-->
                <!--&lt;!&ndash; note that the leading "/" is required for pathPrefix&ndash;&gt;-->
                <!--&lt;!&ndash; Accepts URIs that begin with "example://gizmos” &ndash;&gt;-->
                <!--<data android:scheme="tmdb"-->
                    <!--android:host="movies" />-->

            <!--</intent-filter>-->
        </activity>

        <activity android:name=".SelectWebActivity">

        </activity>
    </application>
</manifest>